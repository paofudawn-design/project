## Example environment variables for Docker/local development
#
# Copy to `.env` for local (non-docker) usage if needed.
# In docker-compose, these are typically provided via `environment:`.

# Server
PORT=8000
NODE_ENV=development

# MongoDB
DB_URL=mongodb://localhost:27017/my-employees

# JWT
JWT_SECRET=change-me-to-a-strong-secret

